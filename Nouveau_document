https://github.com/ssloy/tinyrenderer/wiki
https://github.com/ssloy/walkup

ligne de compilation:
g++ *.cpp `sdl-config --cflags --libs` -o test -g


*.cpp compile tous les fichiers cpp du dossier
test est le nom du fichier exe (peut être changer)

exécuter avec ./test


afficher les lignes sur emacs:

ALT x puis taper linum-mode

indenter:

sélectionner la partie à indenter:

ALT x puis indent-region
